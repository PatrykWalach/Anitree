import MediaItemLoadingDescription from '@/components/MediaItemLoadingDescription.vue'

import { matchSnapshot } from './utils'

describe('MediaItemLoadingDescription', () => {
  matchSnapshot(MediaItemLoadingDescription)
})
